services:
  api:
    build: ..
    ports:
      - "8000:8000"
    volumes:
      - ../data:/app/data
      - ../outputs:/app/outputs
      - ../state:/app/state
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]
